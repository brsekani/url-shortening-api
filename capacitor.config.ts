import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'url-shortening-api',
  webDir: 'dist/url-shortening-api/browser',
};

export default config;
